"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.CompoundTask = void 0;
const DFTask_1 = require("./DFTask");
class CompoundTask extends DFTask_1.DFTask {
    constructor(children, action) {
        super(false, action);
        this.children = children;
        this.action = action;
        children.map((c) => (c.parent = this));
        this.firstError = undefined;
        if (children.length == 0) {
            const message = "When constructing a CompoundTask (such as Task.all() or Task.any()), you must specify at least one Task.";
            throw new Error(message);
        }
    }
    handleCompletion(child, executor) {
        if (!this.isPlayed) {
            this.isPlayed = child.isPlayed;
        }
        this.trySetValue(child, executor);
    }
}
exports.CompoundTask = CompoundTask;
//# sourceMappingURL=CompoundTask.js.map